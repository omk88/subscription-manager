model User {
  id            String    @id @default(cuid())
  email         String    @unique
  password      String?
  sessions      Session[]
}

model Session {
  id        String   @id
  userId    String
  expiresAt DateTime
  user      User     @relation(fields: [userId], references: [id])
}